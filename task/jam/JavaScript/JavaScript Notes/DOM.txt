DOM - Document Object Model
allows to manipulate html document
loads once the page loads
-collection of nodes/hierarchy
every element of dom is an object

elements of html represented by javascript as object
every element is an element object

window object

console.dir(window) - window directory/ browser directory
console.dir(document) - html page element directory


//upon loading the page it will show the body of the html and its functions
window.addEventListener("load", function(){


})


id - #
class = .

Element Selector

getElementById

create a variable and use the selector to access the element

let name = document.getElementById("<id_tag_in_html">);

.innerHTML = modify the content of an element

these following selectors treated each element as an array element
.getElementsByClassName = selects element within the same class
.getElementsByTagName = select all elements that has the tag


onlick & function

onclick - can only call one event assigned to an element

btn.onclick = <function_name>();

function <function_name>(){
    action here...
}

addEventListener - can call multiple event assigned to an element

this is similar to the above example
btn.addEventListener("click", function(e){
    action here...
});
.querySelector - accesses the first element only return the 1st element that matches the soecified selector
.querySelectorAll - access all element and throw it as an array

.childNodes - list out the main tags on the selected element block
.nodeName

.appendChild() - sticks the child node for html element
.append() - append the mother child of the element
.remove() - delete specified element
.replaceChild() - replace/overwrite an element
.innerHTML() - html tag returns htmkl content of an element including the tags/attr and text node
.innerText() - content inside HTML tag
.prepend() - at the beginning
.setAttribute() - adds or create a new attribute for an html element
.textContent() - list all values under the list document element


change css property using javascript
if a css property is used as 
ex. style="background-color: blue;"; ----> .style.backgroundColor = blue;
style="color: blue;"; ----> .style.color = blue;

